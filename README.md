## Dump IPA file from jailbroken Iphone/Ipad via SSH

Run ```pip install -r requirements.txt``` and change SSH host, username, password before run ipa-dump.py
```python
host = ''
username = ''
password = ''
```
